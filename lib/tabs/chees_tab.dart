import 'package:flutter/material.dart';

class CheesTab extends StatelessWidget {
  const CheesTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('CHEES TAB'),
      ),
    );
  }
}