import 'package:flutter/material.dart';

class PieTab extends StatelessWidget {
  const PieTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('PIE TAB'),
      ),
    );
  }
}