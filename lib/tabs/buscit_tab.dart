import 'package:flutter/material.dart';

class BuscitTab extends StatelessWidget {
  const BuscitTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('BUSCIT TAB'),
    );
  }
}